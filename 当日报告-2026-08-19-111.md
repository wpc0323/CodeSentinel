# 当日报告

| 字段     | 内容                                          |
| ------ | ------------------------------------------- |
| 课题编号   |IB00434                                 |
| 课题名称   | 程序设计平台反人工智能作弊   |
| 完成方式   | 双人组：王嘉乐、吴鹏楚               |
| 日期     | 2026-08-19（第 1 天 / 共 9 天）                    |
| 阶段     | 立项与系统骨架（S                                |

## 基本信息

- **课题名称**：程序设计平台反人工智能作弊
- **完成方式**：双人组。初步分工：王嘉乐负责确定项目总流程，调查已有AI防作弊手段，初步确定可采纳手段；吴鹏楚负责题库设计、前端页面、防护机制文本材料与报告撰写。两人共同负责对照实验执行与数据复核。
- **参考项目**：CCPCOJ（一站式 XCPC 比赛系统）。

## 今日目标

今天要明确的边界有三条：(1) 9 天结束时能演示、能验收的最小功能集合是什么；(2) 哪些功能明确不做，防止范围扩大；(3) 用一条主路径和若干可核验的验收说明把"做完"的标准钉死，避免后续靠形容词判断进度。

## 必做与不做

### 必做（9 天结束时要能演示、能验收）

1. **参考项目与 OJ 业务场景分析（M1）**：以 CCPCOJ 为参照，分析其技术栈（JS/PHP/C++/MySQL/Docker）、业务模块（用户端/管理端/赛事系统/核心评测）与判题闭环；提炼 OJ 业务主线"题目创建→展示→提交→沙箱判题→结果反馈→排名"，得出"防护机制应作用于题目展示与信息分发环节"的结论，给出业务流程图。
2. **简易评测 Web 页（M2）**：本地可运行的 Web 应用，含题目列表、题目详情、代码提交、判定结果四页；判题用受限子进程 + 隐藏测试数据比对，支持 AC/WA/TLE/RE/CE 五种判定。
3. **防护机制一·同构多版本（M3）**：每道题至少 3 个同构版本（背景故事/数值/变量名/样例不同），按会话确定性分配；页面有一键开关，刷新或重置会话能看到不同版本。
4. **防护机制二·隐藏干扰信息（M4）**：含两个子模式——干扰注入（AI 若采纳可被规则检测）与约束隐藏（跳过题干中标记为可隐藏的关键句，判题数据里埋针对该约束的边界测试点）；页面有三态切换（原始/干扰/隐藏）。
5. **对照实验框架（M5）**：批量脚本枚举（题目 × 防护状态 × 模型 × 重复），自动调用 AI、提取代码、判题、记录误导；原始数据（AI 回答、代码、判定、时间戳）入 SQLite；统计分析输出通过率/误导率/卡方检验。
6. **结课大报告（M6）**：实验类结构，含背景、实验设计、原始数据摘要、统计结论与反思。
7. **过程文档（M7）**：立项书、每日报告均为 markdown。

### 不做（明确排除，防止范围扩大）

- **N1 完整用户体系**：不做注册/登录/权限，本地单会话 + 实验会话 ID 即可。
- **N2 生产级沙箱**：不做 cgroup/namespace/seccomp 全套；判题用受限子进程，无 g++ 时降级报 CE。
- **N3 赛事配套**：不做滚榜/气球/打印/抽签/封榜。
- **N4 反作弊基础设施**：不做代码相似度检测、IP/设备指纹。
- **N5 公网部署**：仅本地 127.0.0.1 演示。
- **N6 模型训练**：不训练/微调，仅调用现有模型 API。
- **N7 防护完备性**：不追求对所有对抗手段免疫，只验证机制本身的有效性趋势。

### 数据范围限定

- 题目集：8 道自拟题（入门 3 + 中档 5），覆盖模拟/字符串/贪心/栈/DP/BFS/堆；不用公开原题避免数据污染。
- 模型：2 个（按可用 API 选定，版本快照固定）。
- 重复次数：每格 3 次。
- 最小规模：8 × 4 × 2 × 3 = 192 次 AI 求解。

## 主路径与验收

### 主路径（用户进入到看到结果）

1. 启动 `python start.py` → 浏览器打开题目列表页；
2. 进入某题 → 默认"原始版"→ 在代码框写一份 Python 解 → 点"提交判题"→ 看到逐测试点判定（AC/WA/TLE/RE/CE）；
3. 切到"P1 同构多版本"→ 点"换一个同构版本"→ 题面数值/背景/样例变化；
4. 切到"P2a 干扰注入"→ 题干末尾出现"旧系统要求对 1e9+7 取模"等蜜饵段落；
5. 切到"P2b 约束隐藏"→ 题干中"区分大小写""首尾相接视为冲突"等关键句消失；
6. 运行 `python start.py --mock-experiment` → 实验数据页显示各防护状态通过率/误导率统计。

### 验收说明（可按步骤核对）

- **验收 1（判题闭环）**：在 P001 糖果清点页，用正确求和解提交 → 应得 AC 且 4/4 测试点通过；把解改成"先求和再对 1e9+7 取模"提交 → 应得 WA，且失败测试点名 `bigsum`（边界数据：三个 20 亿元的和大于 1e9+7）。
- **验收 2（机制一版本切换）**：在 P001 切到 P1 模式，连续点 3 次"换一个同构版本"→ 每次的版本标签应交替出现 V1 货运版 / V2 图书版，且每次都和上一次不同（避免连续命中同版本）；提交记录页仍能看到之前的提交。
- **验收 3（机制二干扰可检测）**：在 P002 切到 P2a 干扰注入 → 题干末尾出现"先把字母统一转小写再判断"的蜜饵段落；若提交的代码里调用了 `.lower()` → 在 `case-mix`（如 `aAb`）测试点上应 WA，实验记录里该次求解的误导字段应被标为 1。
- **验收 4（实验可复现）**：运行 `python start.py --mock-experiment` 后再运行 `python start.py --analyze` → `report/` 目录下应生成 `table_overall.csv`、`statistics.txt`，且 `statistics.txt` 含 P0-P3 各状态的通过率与卡方检验 p 值。

## 风险与依赖

| 风险 | 影响 | 应对 |
|---|---|---|
| 本机无 g++ 编译器 | C++ 提交无法判题，降级为 CE | 实验脚本固定用 Python3；演示时语言下拉框默认 Python；如需 C++ 可装 MinGW |
| 真实模型 API 需付费/有配额 | 正式实验无法在 9 天内跑完 192 次 | 先用 `--mock` 验证管线；正式实验可缩小到 4 题 × 4 状态 × 1 模型 × 3 次 = 48 次 |
| 小样本统计不显著 | p 值可能 > 0.05，结论不强 | 报告中如实标注"小样本谨慎解读"；以通过率差值与误导率描述性结论为主，显著性检验为辅 |
| 开发者即被测模型（题解泄露） | 防护对"已见题模型"效果有限，机制一可能无效 | 这恰是立项书 7.1 预设的边界；正式实验换外部未参与开发的模型，本数据作为"最坏场景上界"参照 |
| 题库期望输出算错 | 判题基准错误，实验结论失效 | `build_problems.py` 内置断言：手写样例的期望值与标准解实跑结果不符时直接报错终止，不生成错误题库 |

## 今日完成与自检

今天实际确定并完成的内容：

1. **立项书 v1.0**：含背景、CCPCOJ 分析、RQ1-RQ4、必做 M1-M7 / 不做 N1-N7 / 验收 A1-A6、MVP 范围、两种机制设计、实验设计、里程碑。
2. **参考项目与 OJ 业务场景分析（M1，已完成）**：完成 CCPCOJ 直链分析，梳理出题目生命周期（创作→入库→展示→提交→评测）与判题闭环（编译+沙箱运行+输出比对），结论"防护机制应作用于展示与信息分发环节"。
3. **系统骨架已搭完**（原计划后续天数推进，今日提前完成）：
   - 题库生成器 `tools/build_problems.py`：8 道自拟题 × 3 同构版本 × 测试数据，期望输出由标准解实跑得出，样例断言全过；
   - 后端 `server/app.py`（FastAPI）：题目/题面/提交/记录/实验摘要 API，防护机制在展示层生效；
   - 判题引擎 `server/judge/engine.py`：受限子进程 + 超时 + 输出比对；
   - 前端 4 页：题目列表/详情（防护开关+换版本+防窥模式）/提交记录/实验数据看板；
   - 实验脚本 `experiment/run_experiment.py` + `experiment/analyze.py`：批量调度+统计+卡方检验。
3. **端到端验证对照**：

| 验收说明 | 今日状态 |
|---|---|
| 验收 1 判题闭环 | ✅ 正确解 AC 4/4；取模解 WA 落 `bigsum`；死循环 TLE；语法错 CE |
| 验收 2 版本切换 | ✅ 连续换版本 V1↔V2 交替无重复；刷新页面换版本；历史提交保留 |
| 验收 3 干扰可检测 | ✅ P002 lower 解在 `case-mix` 上 WA；误导字段标 1 |
| 验收 4 实验可复现 | ✅ mock 120 格跑通；`report/` 含 4 个 CSV + statistics.txt + 图表（已按需改为 --no-charts 纯文本） |

今日还做了一次 AI 自测（把题面喂给开发本系统的 AI 自身，60 格），结果：P0/P1 均 100%（机制一对已见题模型无效，符合预设边界），P2/P3 因约束隐藏降到 95%，唯一失分是 P004 活动安排的约束隐藏格——按经典模板默认"端点可重合"被边界测试 `touch` 抓中。这组数据作为"最坏场景上界"留档。

> 范围变更记录：今日将题库从原计划 10 题精简为 8 题（删除 P004 活动安排、P009 严格递增路线，因其机制二陷阱与其它题有重叠，保留 8 题仍覆盖全部考点且通过立项书下界"≥8 道"）。编号已重新连续化（P001-P008）。

## 问题、计划与 AI 沟通

### 不成功的沟通

1. **首次要求"做出整个项目"**：AI 直接开始写代码，但中途题库生成器的样例期望值有 5 处手算错误（如 P004 的 BFS 距离算成 5 实为 6、P009 的 LIS 算成 5 实为 4）。后来改为让生成器内置断言（标准解实跑结果必须等于手写期望值，不等就报错终止），再跑一次自动暴露全部错误，修正后一次通过。教训：不能盲信 AI 的手算，必须用程序自动验证。
2. **要求"把数据喂给你自己测试"**：AI 一开始用 mock 模拟数据（人为设计的通过率），被我指出"不要模拟数据"后才改为真实作答。但 AI 作答时已见过题库源码，属于题解泄露场景——这本身是立项书预设的边界，AI 也在报告里如实标注了，不能当作正式结论。
3. **要求"切换到其他网站就刷新"**：AI 第一版用 `visibilitychange` 但没防抖，标签栏短暂闪烁就触发换版本。后来加"离开超过 1 秒才触发"的防抖逻辑才可用。


### 比较有效的沟通

1. **明确"作用于展示层、不改变判题逻辑"这一约束**：这条约束让 AI 在实现机制二时把扰动放在 `build_view`（题面组装）、判题仍用 `view_tests`（原始测试数据），避免了"改了题面导致判题基准也变"的常见错误。
2. **要求"同 token 内版本稳定、不同 token 版本不同"**：这条确定性约束让 AI 用 SHA256 哈希分配版本，既保证刷新换版本，又保证同一 view_token 内提交判题版本一致，避免了"展示的是 V1 但判题用 V2"的错位。
3. **要求"一定切换，不能连续命中同一个"**：AI 据此加了 `avoid_key` 参数，换版本时把当前版本 key 传给后端排除，保证 2 个候选版本时一定交替。

### 明日安排

1. 配置真实模型 API，先小规模试跑 4 题 × 2 状态 × 1 次 = 8 次验证 API 通断；
2. 若 API 通，跑全量 8 × 4 × 2 × 3 = 192 次正式实验；
3. 人工复核 2a 干扰格的原始回答（规则判会漏判"识破蜜饵"的情况，需人工标注补充）。
