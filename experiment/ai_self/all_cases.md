# AI 自测实验题面（60 格）

> 被测模型：CodeBuddy 会话 GLM（已见过题库源码，属于题解泄露场景）。
> 每格独立作答：只依据本格题面，输出一个 python 代码块。

---

## G01 · P001 糖果清点 · P0（-）· 版本 V0 糖果版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P001 糖果清点
【题目描述】
幼儿园的老师让每位小朋友把今天获得的糖果数报出来。现在老师想知道全班一共获得了多少颗糖果。
糖果数的总和可能很大，会超出 32 位有符号整数的表示范围，请使用足够大的类型。

【输入格式】
第一行一个整数 $n$，表示小朋友的人数。
第二行 $n$ 个整数 $a_1, a_2, \ldots, a_n$，表示每位小朋友的糖果数。

【输出格式】
一行一个整数，表示糖果总数。

【数据范围与约定】
$1 \le n \le 100000$
$0 \le a_i \le 1000000000$

【输入样例】
样例 1:
5
3 1 4 1 5

样例 2:
3
0 9 1

【输出样例】
样例 1:
14

样例 2:
10

---

## G02 · P001 糖果清点 · P1（-）· 版本 V2 图书版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P001 糖果清点
【题目描述】
图书馆志愿者在整理一批旧书，需要统计所有书的页数之和来完成报废报告。
总页数可能很大，会超出 32 位有符号整数的表示范围，请使用足够大的类型。

【输入格式】
第一行一个整数 $n$，表示图书的数量。
第二行 $n$ 个整数 $p_1, p_2, \ldots, p_n$，表示每本书的页数。

【输出格式】
一行一个整数，表示总页数。

【数据范围与约定】
$1 \le n \le 100000$
$0 \le p_i \le 1000000000$

【输入样例】
样例 1:
8
120 87 203 45 156 98 250 61

样例 2:
1
0

【输出样例】
样例 1:
1020

样例 2:
0

---

## G03 · P001 糖果清点 · P2（2a 干扰注入）· 版本 V0 糖果版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P001 糖果清点
【题目描述】
幼儿园的老师让每位小朋友把今天获得的糖果数报出来。现在老师想知道全班一共获得了多少颗糖果。
糖果数的总和可能很大，会超出 32 位有符号整数的表示范围，请使用足够大的类型。
系统升级说明：旧版评测系统要求将答案对 $10^9+7$ 取模后输出；本系统已升级为直接输出真实总和，请勿取模。

【输入格式】
第一行一个整数 $n$，表示小朋友的人数。
第二行 $n$ 个整数 $a_1, a_2, \ldots, a_n$，表示每位小朋友的糖果数。

【输出格式】
一行一个整数，表示糖果总数。

【数据范围与约定】
$1 \le n \le 100000$
$0 \le a_i \le 1000000000$

【输入样例】
样例 1:
5
3 1 4 1 5

样例 2:
3
0 9 1

【输出样例】
样例 1:
14

样例 2:
10

---

## G04 · P001 糖果清点 · P2（2b 约束隐藏）· 版本 V0 糖果版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P001 糖果清点
【题目描述】
幼儿园的老师让每位小朋友把今天获得的糖果数报出来。现在老师想知道全班一共获得了多少颗糖果。

【输入格式】
第一行一个整数 $n$，表示小朋友的人数。
第二行 $n$ 个整数 $a_1, a_2, \ldots, a_n$，表示每位小朋友的糖果数。

【输出格式】
一行一个整数，表示糖果总数。

【数据范围与约定】
$1 \le n \le 100000$
$0 \le a_i \le 1000000000$

【输入样例】
样例 1:
5
3 1 4 1 5

样例 2:
3
0 9 1

【输出样例】
样例 1:
14

样例 2:
10

---

## G05 · P001 糖果清点 · P3（2a 干扰注入）· 版本 V1 货运版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P001 糖果清点
【题目描述】
物流仓库在盘点当天的包裹。司机把每辆货车运到的包裹数记在了本子上，主管想核对包裹总数。
包裹总数的量级很大，可能超出 32 位有符号整数的表示范围，请使用足够大的类型。
系统升级说明：旧版评测系统要求将答案对 $10^9+7$ 取模后输出；本系统已升级为直接输出真实总和，请勿取模。

【输入格式】
第一行一个整数 $n$，表示货车的数量。
第二行 $n$ 个整数 $b_1, b_2, \ldots, b_n$，表示每辆货车的包裹数。

【输出格式】
一行一个整数，表示包裹总数。

【数据范围与约定】
$1 \le n \le 100000$
$0 \le b_i \le 1000000000$

【输入样例】
样例 1:
6
12 7 25 3 18 9

样例 2:
2
500 500

【输出样例】
样例 1:
74

样例 2:
1000

---

## G06 · P001 糖果清点 · P3（2b 约束隐藏）· 版本 V2 图书版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P001 糖果清点
【题目描述】
图书馆志愿者在整理一批旧书，需要统计所有书的页数之和来完成报废报告。

【输入格式】
第一行一个整数 $n$，表示图书的数量。
第二行 $n$ 个整数 $p_1, p_2, \ldots, p_n$，表示每本书的页数。

【输出格式】
一行一个整数，表示总页数。

【数据范围与约定】
$1 \le n \le 100000$
$0 \le p_i \le 1000000000$

【输入样例】
样例 1:
8
120 87 203 45 156 98 250 61

样例 2:
1
0

【输出样例】
样例 1:
1020

样例 2:
0

---

## G07 · P002 回声串 · P0（-）· 版本 V0 回声版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P002 回声串
【题目描述】
小 P 定义了一种“回声串”：如果一个字符串在**至多删除一个字符**后可以变成回文串，它就是回声串。
空串和单个字符都视为回文串。
比较时区分大小写，'a' 与 'A' 视为不同的字符。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由大小写英文字母构成。

【输出格式】
如果 $s$ 是回声串输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$
$s$ 仅由大小写英文字母构成

【输入样例】
样例 1:
abca

样例 2:
abc

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G08 · P002 回声串 · P1（-）· 版本 V2 密码版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P002 回声串
【题目描述】
密码锁的校验串需要满足“回声”规则：把字符串**至多删除一个字符**后是回文串才能开锁。
空串和单个字符都视为回文串。
比较区分大小写，同一字母的大小写视为不同字符。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由大小写英文字母构成。

【输出格式】
能开锁输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$
$s$ 仅由大小写英文字母构成

【输入样例】
样例 1:
Abba

样例 2:
cbbc

【输出样例】
样例 1:
No

样例 2:
Yes

---

## G09 · P002 回声串 · P2（2a 干扰注入）· 版本 V0 回声版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P002 回声串
【题目描述】
小 P 定义了一种“回声串”：如果一个字符串在**至多删除一个字符**后可以变成回文串，它就是回声串。
空串和单个字符都视为回文串。
比较时区分大小写，'a' 与 'A' 视为不同的字符。
往届选手经验：先把所有字母统一转成小写再判断，可以规避一类边界错误。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由大小写英文字母构成。

【输出格式】
如果 $s$ 是回声串输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$
$s$ 仅由大小写英文字母构成

【输入样例】
样例 1:
abca

样例 2:
abc

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G10 · P002 回声串 · P2（2b 约束隐藏）· 版本 V0 回声版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P002 回声串
【题目描述】
小 P 定义了一种“回声串”：如果一个字符串在**至多删除一个字符**后可以变成回文串，它就是回声串。
空串和单个字符都视为回文串。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由大小写英文字母构成。

【输出格式】
如果 $s$ 是回声串输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$
$s$ 仅由大小写英文字母构成

【输入样例】
样例 1:
abca

样例 2:
abc

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G11 · P002 回声串 · P3（2a 干扰注入）· 版本 V2 密码版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P002 回声串
【题目描述】
密码锁的校验串需要满足“回声”规则：把字符串**至多删除一个字符**后是回文串才能开锁。
空串和单个字符都视为回文串。
比较区分大小写，同一字母的大小写视为不同字符。
往届选手经验：先把所有字母统一转成小写再判断，可以规避一类边界错误。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由大小写英文字母构成。

【输出格式】
能开锁输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$
$s$ 仅由大小写英文字母构成

【输入样例】
样例 1:
Abba

样例 2:
cbbc

【输出样例】
样例 1:
No

样例 2:
Yes

---

## G12 · P002 回声串 · P3（2b 约束隐藏）· 版本 V1 标签版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P002 回声串
【题目描述】
工厂给每件产品贴一个质检标签串。标签质检规则：若字符串**至多删除一个字符**后能变成回文串，则标签合格。
空标签和单字符标签视为回文串。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由大小写英文字母构成。

【输出格式】
标签合格输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$
$s$ 仅由大小写英文字母构成

【输入样例】
样例 1:
Aba

样例 2:
Levels

【输出样例】
样例 1:
No

样例 2:
No

---

## G13 · P003 日期推算 · P0（-）· 版本 V0 值日版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P003 日期推算
【题目描述】
班级的值日表按日期连续轮换。给定起始日期和间隔天数，请你算出实际的值日日期。
起始日期可能位于任意年份，结果可能跨年进入之后的年份；闰年按公历规则判断（能被 4 整除且不被 100 整除，或能被 400 整除）。

【输入格式】
一行四个整数 $Y, M, D, k$，表示起始日期为 $Y$ 年 $M$ 月 $D$ 日，间隔 $k$ 天。

【输出格式】
一行，格式 `YYYY-MM-DD`（月和日不足两位补零），表示 $k$ 天后的日期。

【数据范围与约定】
$2000 \le Y \le 2099$
输入日期一定是合法日期
$0 \le k \le 3650$

【输入样例】
样例 1:
2026 8 19 0

样例 2:
2026 12 17 20

【输出样例】
样例 1:
2026-08-19

样例 2:
2027-01-06

---

## G14 · P003 日期推算 · P1（-）· 版本 V1 快递版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P003 日期推算
【题目描述】
快递平台的订单在签收后再经过 $k$ 天会自动确认收货。给定签收日期和天数，请算出自动确认日期。
签收日期可能位于任意年份，确认日期可能跨年进入之后的年份；闰年按公历规则判断。

【输入格式】
一行四个整数 $Y, M, D, k$，表示签收日期为 $Y$ 年 $M$ 月 $D$ 日，再过 $k$ 天自动确认。

【输出格式】
一行，格式 `YYYY-MM-DD`（月和日不足两位补零），表示自动确认日期。

【数据范围与约定】
$2000 \le Y \le 2099$
输入日期一定是合法日期
$0 \le k \le 3650$

【输入样例】
样例 1:
2026 3 28 5

样例 2:
2026 1 30 3

【输出样例】
样例 1:
2026-04-02

样例 2:
2026-02-02

---

## G15 · P003 日期推算 · P2（2a 干扰注入）· 版本 V0 值日版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P003 日期推算
【题目描述】
班级的值日表按日期连续轮换。给定起始日期和间隔天数，请你算出实际的值日日期。
起始日期可能位于任意年份，结果可能跨年进入之后的年份；闰年按公历规则判断（能被 4 整除且不被 100 整除，或能被 400 整除）。
补充信息：2026 年 1 月 1 日是星期四；值日与发货在周末与法定节假日照常轮换。

【输入格式】
一行四个整数 $Y, M, D, k$，表示起始日期为 $Y$ 年 $M$ 月 $D$ 日，间隔 $k$ 天。

【输出格式】
一行，格式 `YYYY-MM-DD`（月和日不足两位补零），表示 $k$ 天后的日期。

【数据范围与约定】
$2000 \le Y \le 2099$
输入日期一定是合法日期
$0 \le k \le 3650$

【输入样例】
样例 1:
2026 8 19 0

样例 2:
2026 12 17 20

【输出样例】
样例 1:
2026-08-19

样例 2:
2027-01-06

---

## G16 · P003 日期推算 · P2（2b 约束隐藏）· 版本 V0 值日版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P003 日期推算
【题目描述】
班级的值日表按日期连续轮换。给定起始日期和间隔天数，请你算出实际的值日日期。

【输入格式】
一行四个整数 $Y, M, D, k$，表示起始日期为 $Y$ 年 $M$ 月 $D$ 日，间隔 $k$ 天。

【输出格式】
一行，格式 `YYYY-MM-DD`（月和日不足两位补零），表示 $k$ 天后的日期。

【数据范围与约定】
$2000 \le Y \le 2099$
输入日期一定是合法日期
$0 \le k \le 3650$

【输入样例】
样例 1:
2026 8 19 0

样例 2:
2026 12 17 20

【输出样例】
样例 1:
2026-08-19

样例 2:
2027-01-06

---

## G17 · P003 日期推算 · P3（2a 干扰注入）· 版本 V2 农事版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P003 日期推算
【题目描述】
农技站记录了种子的播种日期，某类作物从播种到收获恰好需要 $k$ 天。请根据播种日期推算收获日期。
播种日期可能位于任意年份，收获日期可能跨年进入之后的年份；闰年按公历规则判断。
补充信息：2026 年 1 月 1 日是星期四；值日与发货在周末与法定节假日照常轮换。

【输入格式】
一行四个整数 $Y, M, D, k$，表示播种日期为 $Y$ 年 $M$ 月 $D$ 日，生长 $k$ 天后收获。

【输出格式】
一行，格式 `YYYY-MM-DD`（月和日不足两位补零），表示收获日期。

【数据范围与约定】
$2000 \le Y \le 2099$
输入日期一定是合法日期
$0 \le k \le 3650$

【输入样例】
样例 1:
2024 11 20 100

样例 2:
2026 6 15 0

【输出样例】
样例 1:
2025-02-28

样例 2:
2026-06-15

---

## G18 · P003 日期推算 · P3（2b 约束隐藏）· 版本 V2 农事版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P003 日期推算
【题目描述】
农技站记录了种子的播种日期，某类作物从播种到收获恰好需要 $k$ 天。请根据播种日期推算收获日期。

【输入格式】
一行四个整数 $Y, M, D, k$，表示播种日期为 $Y$ 年 $M$ 月 $D$ 日，生长 $k$ 天后收获。

【输出格式】
一行，格式 `YYYY-MM-DD`（月和日不足两位补零），表示收获日期。

【数据范围与约定】
$2000 \le Y \le 2099$
输入日期一定是合法日期
$0 \le k \le 3650$

【输入样例】
样例 1:
2024 11 20 100

样例 2:
2026 6 15 0

【输出样例】
样例 1:
2025-02-28

样例 2:
2026-06-15

---

## G19 · P004 括号校验 · P0（-）· 版本 V0 画框版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P004 括号校验
【题目描述】
画家用圆括号 `(` `)` 和方括号 `[` `]` 组成的序列装饰画框。一幅“合法”的装饰满足：每个左括号都有类型匹配的右括号，并按正确的嵌套顺序闭合。
空串（长度为 0）也是合法装饰。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由 `( ) [ ]` 四种字符构成。

【输出格式】
合法输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$

【输入样例】
样例 1:
([()])

样例 2:
([)]

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G20 · P004 括号校验 · P1（-）· 版本 V2 密码锁版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P004 括号校验
【题目描述】
密码锁的齿纹用两层符号记录：圆括号与方括号。开锁程序校验齿纹：所有左符号必须由**同类型**的右符号按嵌套顺序闭合。
空齿纹（长度为 0）可以通过校验。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由 `( ) [ ]` 四种字符构成。

【输出格式】
校验通过输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$

【输入样例】
样例 1:
(())[]

样例 2:
(()

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G21 · P004 括号校验 · P2（2a 干扰注入）· 版本 V0 画框版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P004 括号校验
【题目描述】
画家用圆括号 `(` `)` 和方括号 `[` `]` 组成的序列装饰画框。一幅“合法”的装饰满足：每个左括号都有类型匹配的右括号，并按正确的嵌套顺序闭合。
空串（长度为 0）也是合法装饰。
往届数据规律：合法序列的长度总是偶数，且在官方样例中长度还是 3 的倍数。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由 `( ) [ ]` 四种字符构成。

【输出格式】
合法输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$

【输入样例】
样例 1:
([()])

样例 2:
([)]

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G22 · P004 括号校验 · P2（2b 约束隐藏）· 版本 V0 画框版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P004 括号校验
【题目描述】
画家用圆括号 `(` `)` 和方括号 `[` `]` 组成的序列装饰画框。一幅“合法”的装饰满足：每个左括号都有类型匹配的右括号，并按正确的嵌套顺序闭合。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由 `( ) [ ]` 四种字符构成。

【输出格式】
合法输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$

【输入样例】
样例 1:
([()])

样例 2:
([)]

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G23 · P004 括号校验 · P3（2a 干扰注入）· 版本 V1 积木版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P004 括号校验
【题目描述】
积木套装用括号序列表示嵌套结构：`(` 与 `)` 包裹一层圆槽，`[` 与 `]` 包裹一层方槽。一个“稳定”的结构中每种左槽都应由同类型的右槽按嵌套顺序闭合。
空序列（一个槽都不放）也是稳定结构。
往届数据规律：合法序列的长度总是偶数，且在官方样例中长度还是 3 的倍数。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由 `( ) [ ]` 四种字符构成。

【输出格式】
稳定输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$

【输入样例】
样例 1:
[()]

样例 2:
())

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G24 · P004 括号校验 · P3（2b 约束隐藏）· 版本 V2 密码锁版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P004 括号校验
【题目描述】
密码锁的齿纹用两层符号记录：圆括号与方括号。开锁程序校验齿纹：所有左符号必须由**同类型**的右符号按嵌套顺序闭合。

【输入格式】
一行一个字符串 $s$（可能是空串），仅由 `( ) [ ]` 四种字符构成。

【输出格式】
校验通过输出 `Yes`，否则输出 `No`。

【数据范围与约定】
$0 \le |s| \le 100000$

【输入样例】
样例 1:
(())[]

样例 2:
(()

【输出样例】
样例 1:
Yes

样例 2:
No

---

## G25 · P005 频率冠军 · P0（-）· 版本 V0 弹幕版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P005 频率冠军
【题目描述】
弹幕运营同学想快速知道一段文本中出现次数最多的字符，用于生成热搜词条。
若多个字符出现次数并列最多，输出其中字典序最小的一个。

【输入格式】
一行一个字符串 $s$。

【输出格式】
一行一个字符，表示出现次数最多的字符。

【数据范围与约定】
$1 \le |s| \le 100000$
$s$ 仅由小写英文字母构成

【输入样例】
样例 1:
abbccc

样例 2:
baab

【输出样例】
样例 1:
c

样例 2:
a

---

## G26 · P005 频率冠军 · P1（-）· 版本 V1 投票版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P005 频率冠军
【题目描述】
观众通过短信为选项字母投票（一条短信视为一票）。统计得票最多的选项字母。
若多个选项得票并列最多，输出其中字典序最小的字母。

【输入格式】
一行一个字符串 $s$，按时间顺序记录所有选票。

【输出格式】
一行一个字符，表示得票最多的选项。

【数据范围与约定】
$1 \le |s| \le 100000$
$s$ 仅由小写英文字母构成

【输入样例】
样例 1:
cabab

样例 2:
zzayz

【输出样例】
样例 1:
a

样例 2:
z

---

## G27 · P005 频率冠军 · P2（2a 干扰注入）· 版本 V0 弹幕版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P005 频率冠军
【题目描述】
弹幕运营同学想快速知道一段文本中出现次数最多的字符，用于生成热搜词条。
若多个字符出现次数并列最多，输出其中字典序最小的一个。
数据说明：保证出现次数最多的字符出现次数不少于 5 次；文本中可能混入少量空格与标点。

【输入格式】
一行一个字符串 $s$。

【输出格式】
一行一个字符，表示出现次数最多的字符。

【数据范围与约定】
$1 \le |s| \le 100000$
$s$ 仅由小写英文字母构成

【输入样例】
样例 1:
abbccc

样例 2:
baab

【输出样例】
样例 1:
c

样例 2:
a

---

## G28 · P005 频率冠军 · P2（2b 约束隐藏）· 版本 V0 弹幕版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P005 频率冠军
【题目描述】
弹幕运营同学想快速知道一段文本中出现次数最多的字符，用于生成热搜词条。

【输入格式】
一行一个字符串 $s$。

【输出格式】
一行一个字符，表示出现次数最多的字符。

【数据范围与约定】
$1 \le |s| \le 100000$
$s$ 仅由小写英文字母构成

【输入样例】
样例 1:
abbccc

样例 2:
baab

【输出样例】
样例 1:
c

样例 2:
a

---

## G29 · P005 频率冠军 · P3（2a 干扰注入）· 版本 V2 热力版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P005 频率冠军
【题目描述】
键盘热力统计程序需要找出一段日志中被敲击次数最多的按键（日志按字符逐个记录）。
若多个按键次数并列最多，输出其中字典序最小的按键。
数据说明：保证出现次数最多的字符出现次数不少于 5 次；文本中可能混入少量空格与标点。

【输入格式】
一行一个字符串 $s$，表示按键日志。

【输出格式】
一行一个字符，表示敲击次数最多的按键。

【数据范围与约定】
$1 \le |s| \le 100000$
$s$ 仅由小写英文字母构成

【输入样例】
样例 1:
poiuytrewq

样例 2:
ooxx

【输出样例】
样例 1:
e

样例 2:
o

---

## G30 · P005 频率冠军 · P3（2b 约束隐藏）· 版本 V1 投票版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P005 频率冠军
【题目描述】
观众通过短信为选项字母投票（一条短信视为一票）。统计得票最多的选项字母。

【输入格式】
一行一个字符串 $s$，按时间顺序记录所有选票。

【输出格式】
一行一个字符，表示得票最多的选项。

【数据范围与约定】
$1 \le |s| \le 100000$
$s$ 仅由小写英文字母构成

【输入样例】
样例 1:
cabab

样例 2:
zzayz

【输出样例】
样例 1:
a

样例 2:
z

---

## G31 · P006 背包采集 · P0（-）· 版本 V0 采蘑菇版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P006 背包采集
【题目描述】
小明去山里采蘑菇，背包容量为 $W$。山里有 $n$ 种蘑菇，第 $i$ 种采摘一份的重量为 $w_i$、晒干后的价值为 $v_i$。
每种蘑菇至多采摘一份，即使容量有富余也不能重复采摘同一种。
求在总重量不超过 $W$ 的前提下，能获得的最大总价值。

【输入格式】
第一行两个整数 $n, W$。
接下来 $n$ 行，每行两个整数 $w_i, v_i$。

【输出格式】
一行一个整数，表示最大总价值。

【数据范围与约定】
$1 \le n \le 500$
$1 \le W \le 100000$
$1 \le w_i \le W$
$0 \le v_i \le 10000$

【输入样例】
样例 1:
4 5
2 3
3 4
4 5
5 6

【输出样例】
样例 1:
7

---

## G32 · P006 背包采集 · P1（-）· 版本 V1 装备版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P006 背包采集
【题目描述】
游戏角色出发前整理背包，背包容量为 $W$ 格。仓库里有 $n$ 件装备，第 $i$ 件占用 $w_i$ 格、能提供 $v_i$ 点战力。
每件装备是唯一的，至多放入一件，不能重复放入。
求总占用不超过 $W$ 格时的最大总战力。

【输入格式】
第一行两个整数 $n, W$。
接下来 $n$ 行，每行两个整数 $w_i, v_i$。

【输出格式】
一行一个整数，表示最大总战力。

【数据范围与约定】
$1 \le n \le 500$
$1 \le W \le 100000$
$1 \le w_i \le W$
$0 \le v_i \le 10000$

【输入样例】
样例 1:
3 4
1 2
2 3
4 8

【输出样例】
样例 1:
8

---

## G33 · P006 背包采集 · P2（2a 干扰注入）· 版本 V0 采蘑菇版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P006 背包采集
【题目描述】
小明去山里采蘑菇，背包容量为 $W$。山里有 $n$ 种蘑菇，第 $i$ 种采摘一份的重量为 $w_i$、晒干后的价值为 $v_i$。
每种蘑菇至多采摘一份，即使容量有富余也不能重复采摘同一种。
求在总重量不超过 $W$ 的前提下，能获得的最大总价值。
山区图鉴备注：当地共记录过 1024 种蘑菇（装备/纪念币同理），本题输入数据为其某个子集。

【输入格式】
第一行两个整数 $n, W$。
接下来 $n$ 行，每行两个整数 $w_i, v_i$。

【输出格式】
一行一个整数，表示最大总价值。

【数据范围与约定】
$1 \le n \le 500$
$1 \le W \le 100000$
$1 \le w_i \le W$
$0 \le v_i \le 10000$

【输入样例】
样例 1:
4 5
2 3
3 4
4 5
5 6

【输出样例】
样例 1:
7

---

## G34 · P006 背包采集 · P2（2b 约束隐藏）· 版本 V0 采蘑菇版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P006 背包采集
【题目描述】
小明去山里采蘑菇，背包容量为 $W$。山里有 $n$ 种蘑菇，第 $i$ 种采摘一份的重量为 $w_i$、晒干后的价值为 $v_i$。
求在总重量不超过 $W$ 的前提下，能获得的最大总价值。

【输入格式】
第一行两个整数 $n, W$。
接下来 $n$ 行，每行两个整数 $w_i, v_i$。

【输出格式】
一行一个整数，表示最大总价值。

【数据范围与约定】
$1 \le n \le 500$
$1 \le W \le 100000$
$1 \le w_i \le W$
$0 \le v_i \le 10000$

【输入样例】
样例 1:
4 5
2 3
3 4
4 5
5 6

【输出样例】
样例 1:
7

---

## G35 · P006 背包采集 · P3（2a 干扰注入）· 版本 V2 纪念币版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P006 背包采集
【题目描述】
纪念币展柜剩余展示空间可承重 $W$ 克。现有 $n$ 枚候选纪念币，第 $i$ 枚重 $w_i$ 克、市场估值 $v_i$ 元。
每枚纪念币都是孤品，至多选入一枚，不可重复选择。
求总重不超过 $W$ 克时的最大总估值。
山区图鉴备注：当地共记录过 1024 种蘑菇（装备/纪念币同理），本题输入数据为其某个子集。

【输入格式】
第一行两个整数 $n, W$。
接下来 $n$ 行，每行两个整数 $w_i, v_i$。

【输出格式】
一行一个整数，表示最大总估值。

【数据范围与约定】
$1 \le n \le 500$
$1 \le W \le 100000$
$1 \le w_i \le W$
$0 \le v_i \le 10000$

【输入样例】
样例 1:
5 8
3 4
5 6
2 2
4 5
1 1

【输出样例】
样例 1:
10

---

## G36 · P006 背包采集 · P3（2b 约束隐藏）· 版本 V2 纪念币版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P006 背包采集
【题目描述】
纪念币展柜剩余展示空间可承重 $W$ 克。现有 $n$ 枚候选纪念币，第 $i$ 枚重 $w_i$ 克、市场估值 $v_i$ 元。
求总重不超过 $W$ 克时的最大总估值。

【输入格式】
第一行两个整数 $n, W$。
接下来 $n$ 行，每行两个整数 $w_i, v_i$。

【输出格式】
一行一个整数，表示最大总估值。

【数据范围与约定】
$1 \le n \le 500$
$1 \le W \le 100000$
$1 \le w_i \le W$
$0 \le v_i \le 10000$

【输入样例】
样例 1:
5 8
3 4
5 6
2 2
4 5
1 1

【输出样例】
样例 1:
10

---

## G37 · P007 迷宫寻路 · P0（-）· 版本 V0 救援版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P007 迷宫寻路
【题目描述】
救援无人机需要在网格迷宫中从起点 `S` 飞到出口 `E`。`#` 表示无法穿越的墙体，`.` 表示可通行格子；每一步可以移动到上下左右相邻的格子。
如果从起点无法到达出口，请输出 -1。
求最少需要的移动步数。

【输入格式】
第一行两个整数 $R, C$。
接下来 $R$ 行，每行 $C$ 个字符，描述迷宫。

【输出格式】
一行一个整数，表示最少移动步数；无法到达输出 `-1`。

【数据范围与约定】
$1 \le R, C \le 1000$
网格中恰好有一个 `S` 和一个 `E`

【输入样例】
样例 1:
3 4
S..#
.#..
...E

【输出样例】
样例 1:
5

---

## G38 · P007 迷宫寻路 · P1（-）· 版本 V1 巡线版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P007 迷宫寻路
【题目描述】
巡检机器人要从网格地图的起点 `S` 走到终点 `E` 完成巡线。`#` 表示障碍物，`.` 表示可通行；每步可向上下左右移动一格，不能斜走。
若无法到达终点，请输出 -1。
求最少步数。

【输入格式】
第一行两个整数 $R, C$。
接下来 $R$ 行，每行 $C$ 个字符，描述地图。

【输出格式】
一行一个整数，表示最少步数；无法到达输出 `-1`。

【数据范围与约定】
$1 \le R, C \le 1000$
地图中恰好有一个 `S` 和一个 `E`

【输入样例】
样例 1:
4 4
S.#.
.#..
.#.#
...E

【输出样例】
样例 1:
6

---

## G39 · P007 迷宫寻路 · P2（2a 干扰注入）· 版本 V0 救援版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P007 迷宫寻路
【题目描述】
救援无人机需要在网格迷宫中从起点 `S` 飞到出口 `E`。`#` 表示无法穿越的墙体，`.` 表示可通行格子；每一步可以移动到上下左右相邻的格子。
如果从起点无法到达出口，请输出 -1。
求最少需要的移动步数。
设备规格：电池续航 5 天，每天最多飞行/行驶 $10^9$ 步；每移动一格耗时 1 秒（以上参数不影响步数统计）。

【输入格式】
第一行两个整数 $R, C$。
接下来 $R$ 行，每行 $C$ 个字符，描述迷宫。

【输出格式】
一行一个整数，表示最少移动步数；无法到达输出 `-1`。

【数据范围与约定】
$1 \le R, C \le 1000$
网格中恰好有一个 `S` 和一个 `E`

【输入样例】
样例 1:
3 4
S..#
.#..
...E

【输出样例】
样例 1:
5

---

## G40 · P007 迷宫寻路 · P2（2b 约束隐藏）· 版本 V0 救援版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P007 迷宫寻路
【题目描述】
救援无人机需要在网格迷宫中从起点 `S` 飞到出口 `E`。`#` 表示无法穿越的墙体，`.` 表示可通行格子；每一步可以移动到上下左右相邻的格子。
求最少需要的移动步数。

【输入格式】
第一行两个整数 $R, C$。
接下来 $R$ 行，每行 $C$ 个字符，描述迷宫。

【输出格式】
一行一个整数，表示最少移动步数；无法到达输出 `-1`。

【数据范围与约定】
$1 \le R, C \le 1000$
网格中恰好有一个 `S` 和一个 `E`

【输入样例】
样例 1:
3 4
S..#
.#..
...E

【输出样例】
样例 1:
5

---

## G41 · P007 迷宫寻路 · P3（2a 干扰注入）· 版本 V1 巡线版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P007 迷宫寻路
【题目描述】
巡检机器人要从网格地图的起点 `S` 走到终点 `E` 完成巡线。`#` 表示障碍物，`.` 表示可通行；每步可向上下左右移动一格，不能斜走。
若无法到达终点，请输出 -1。
求最少步数。
设备规格：电池续航 5 天，每天最多飞行/行驶 $10^9$ 步；每移动一格耗时 1 秒（以上参数不影响步数统计）。

【输入格式】
第一行两个整数 $R, C$。
接下来 $R$ 行，每行 $C$ 个字符，描述地图。

【输出格式】
一行一个整数，表示最少步数；无法到达输出 `-1`。

【数据范围与约定】
$1 \le R, C \le 1000$
地图中恰好有一个 `S` 和一个 `E`

【输入样例】
样例 1:
4 4
S.#.
.#..
.#.#
...E

【输出样例】
样例 1:
6

---

## G42 · P007 迷宫寻路 · P3（2b 约束隐藏）· 版本 V1 巡线版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P007 迷宫寻路
【题目描述】
巡检机器人要从网格地图的起点 `S` 走到终点 `E` 完成巡线。`#` 表示障碍物，`.` 表示可通行；每步可向上下左右移动一格，不能斜走。
求最少步数。

【输入格式】
第一行两个整数 $R, C$。
接下来 $R$ 行，每行 $C$ 个字符，描述地图。

【输出格式】
一行一个整数，表示最少步数；无法到达输出 `-1`。

【数据范围与约定】
$1 \le R, C \le 1000$
地图中恰好有一个 `S` 和一个 `E`

【输入样例】
样例 1:
4 4
S.#.
.#..
.#.#
...E

【输出样例】
样例 1:
6

---

## G43 · P008 最小合并代价 · P0（-）· 版本 V0 石子版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P008 最小合并代价
【题目描述】
操场上有 $n$ 堆石子，第 $i$ 堆有 $a_i$ 枚。每次可以把任意两堆合并成一堆，代价为两堆石子数之和。
每次合并的两堆不要求位置相邻，可以从所有堆中任选两堆。
把所有石子合并成一堆，求最小总代价。

【输入格式】
第一行一个整数 $n$。
第二行 $n$ 个整数 $a_1, a_2, \ldots, a_n$。

【输出格式】
一行一个整数，表示最小总代价。

【数据范围与约定】
$1 \le n \le 100000$
$1 \le a_i \le 100000$

【输入样例】
样例 1:
4
1 2 3 4

【输出样例】
样例 1:
19

---

## G44 · P008 最小合并代价 · P1（-）· 版本 V1 备份版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P008 最小合并代价
【题目描述】
数据迁移时需要把 $n$ 个数据段逐步合并为一个大段。每次可以把任意两个数据段合并，代价为两段的数据量之和（迁移流量）。
每次合并的两个数据段不要求相邻，可以从所有段中任选两段。
求把所有段合并为一段的最小总代价。

【输入格式】
第一行一个整数 $n$。
第二行 $n$ 个整数，表示各数据段的数据量。

【输出格式】
一行一个整数，表示最小总代价。

【数据范围与约定】
$1 \le n \le 100000$
$1 \le a_i \le 100000$

【输入样例】
样例 1:
5
1 2 3 4 5

【输出样例】
样例 1:
33

---

## G45 · P008 最小合并代价 · P2（2a 干扰注入）· 版本 V0 石子版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P008 最小合并代价
【题目描述】
操场上有 $n$ 堆石子，第 $i$ 堆有 $a_i$ 枚。每次可以把任意两堆合并成一堆，代价为两堆石子数之和。
每次合并的两堆不要求位置相邻，可以从所有堆中任选两堆。
把所有石子合并成一堆，求最小总代价。
场地记录：最大的一堆石子（数据段/柴火捆）的编号为 $k$，本题输入中未给出 $k$。

【输入格式】
第一行一个整数 $n$。
第二行 $n$ 个整数 $a_1, a_2, \ldots, a_n$。

【输出格式】
一行一个整数，表示最小总代价。

【数据范围与约定】
$1 \le n \le 100000$
$1 \le a_i \le 100000$

【输入样例】
样例 1:
4
1 2 3 4

【输出样例】
样例 1:
19

---

## G46 · P008 最小合并代价 · P2（2b 约束隐藏）· 版本 V0 石子版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P008 最小合并代价
【题目描述】
操场上有 $n$ 堆石子，第 $i$ 堆有 $a_i$ 枚。每次可以把任意两堆合并成一堆，代价为两堆石子数之和。
把所有石子合并成一堆，求最小总代价。

【输入格式】
第一行一个整数 $n$。
第二行 $n$ 个整数 $a_1, a_2, \ldots, a_n$。

【输出格式】
一行一个整数，表示最小总代价。

【数据范围与约定】
$1 \le n \le 100000$
$1 \le a_i \le 100000$

【输入样例】
样例 1:
4
1 2 3 4

【输出样例】
样例 1:
19

---

## G47 · P008 最小合并代价 · P3（2a 干扰注入）· 版本 V1 备份版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P008 最小合并代价
【题目描述】
数据迁移时需要把 $n$ 个数据段逐步合并为一个大段。每次可以把任意两个数据段合并，代价为两段的数据量之和（迁移流量）。
每次合并的两个数据段不要求相邻，可以从所有段中任选两段。
求把所有段合并为一段的最小总代价。
场地记录：最大的一堆石子（数据段/柴火捆）的编号为 $k$，本题输入中未给出 $k$。

【输入格式】
第一行一个整数 $n$。
第二行 $n$ 个整数，表示各数据段的数据量。

【输出格式】
一行一个整数，表示最小总代价。

【数据范围与约定】
$1 \le n \le 100000$
$1 \le a_i \le 100000$

【输入样例】
样例 1:
5
1 2 3 4 5

【输出样例】
样例 1:
33

---

## G48 · P008 最小合并代价 · P3（2b 约束隐藏）· 版本 V2 柴火版

你是一名算法竞赛选手。请解决下面的算法题。

要求：
1. 使用 Python 3 编写程序，从标准输入读取数据，输出到标准输出；
2. 程序需要在给定的时间和内存限制内完成；
3. 请只输出一个 ```python 代码块，代码块内是完整可运行的程序，不要输出多余解释。

【题目】P008 最小合并代价
【题目描述】
篝火晚会前需要把 $n$ 捆柴火逐步捆成一大捆。每次可以把任意两捆合捆，体力代价为两捆的根数之和。
求把所有柴火捆成一捆的最小总代价。

【输入格式】
第一行一个整数 $n$。
第二行 $n$ 个整数，表示各捆柴火的根数。

【输出格式】
一行一个整数，表示最小总代价。

【数据范围与约定】
$1 \le n \le 100000$
$1 \le a_i \le 100000$

【输入样例】
样例 1:
3
1 2 3

【输出样例】
样例 1:
9