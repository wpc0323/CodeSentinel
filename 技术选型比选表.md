# 技术选型比选表
## 一、后端 Web 框架

| 维度 | **FastAPI（已选）** | Flask | Django |
| --- | --- | --- | --- |
| 安装便利性 | `pip install fastapi uvicorn` 即用 | 一个包即用 | 依赖较多 |
| 出问题定位 | Pydantic 自动校验，报错含字段名 | 手动校验易遗漏 | 组件多，堆栈穿过 ORM |
| 异步支持 | 原生 async/await + `run_in_executor` | 需额外扩展 | 较新、文档少 |
| 学习成本 | 低（`/docs` 自动文档，半天上手） | 低但无文档 | 高 |

**选型理由**：Pydantic 内置校验减少 API 调试时间；`Semaphore(2)+run_in_executor` 不阻塞判题；`/docs` 免写接口文档（省 ~1 天）。
**放弃**：Flask（无内置校验/文档）；Django（仅两张表，ORM/Admin 过度，且拖慢定位）。

**风险与对策**：

| 风险 | 对策 |
| --- | --- |
| Python 3.8 无 `asyncio.to_thread`（3.9+） | 改用 `loop.run_in_executor(None, functools.partial(...))` |
| Pydantic v1/v2 版本错配 | 锁定 `fastapi==0.110.3`，用 BaseModel 兼容模式 |

---

## 二、同构版本分配算法

| 维度 | **SHA-256 哈希取模（已选）** | 随机分配 | 轮询 |
| --- | --- | --- | --- |
| 安装便利性 | 零依赖（hashlib） | 零依赖 | 需维护计数器 |
| 会话稳定性 | 高（同 session 同版本） | 无（每次变） | 中 |
| 实验复现性 | 高 | 无 | 高 |
| 换版本支持 | 高（avoid_key 排除） | 天然 | 需跳过逻辑 |

**选型理由**：`sha256("题ID|session")%n` 确定性映射，保证展示版本=判题数据；`avoid_key` 排除当前版本，保证"换一个"必切；无状态服务端。
**放弃**：随机（不可复现）；轮询（需维护状态，且版本可预测）。

**风险与对策**：

| 风险 | 对策 |
| --- | --- |
| 哈希碰撞（不同 session 同版本） | SHA-256 碰撞概率可忽略；只需"大致均匀"不需严格均匀 |
| 仅 2 候选时 avoid 为空 | `filtered` 为空时回退全候选（不排除），保证不返回 None |

---

## 三、信息扰动策略

| 维度 | **标记驱动+展示层过滤（已选）** | LLM 动态改写 | 模板占位符替换 |
| --- | --- | --- | --- |
| 安装便利性 | 零依赖（JSON 预置标记） | 需 LLM API+费用 | 零依赖 |
| 与判题一致性 | 完美（只改展示） | 难保证（易改题意） | 需精心避免语义漂移 |
| 可控性 | 高（逐句 hideable 标记） | 低（输出不可控） | 中 |
| 出问题定位 | 快（直看 JSON 过滤） | 慢（需 diff） | 快 |
| 误导检测 | 原生（蜜饵词规则匹配） | 需额外设计 | 需预埋标记 |

**选型理由**：展示层（`build_view`）与判题层（`view_tests`）彻底分离；distractor 追加蜜饵段+规则检测采纳，hide 过滤 hideable 句+边界测试点失守检测；JSON 结构人工可审。
**放弃**：LLM 改写（288+ 次额外调用、可能改数学约束、不可复现）；模板替换（自然语言相互关联，易语义矛盾）。

**风险与对策**：

| 风险 | 对策 |
| --- | --- |
| 标记遗漏（该隐藏未标 hideable） | review checklist 逐项核对；实验反向验证（hide 通过率应显著低于 original） |
| 蜜饵词太明显被 AI 识破 | 设计为"看似相关实则无关"的伪线索（如引用不存在的定理名） |
| 判题数据漏出（干扰混入测试点） | 干扰/隐藏只作用于 `build_view`，`view_tests` 始终取原始数据，路径隔离 |

---

## 四、实验框架与 AI 接入

| 维度 | **OpenAI 兼容 API+Mock 双模式（已选）** | 仅 Mock | 仅在线 API |
| --- | --- | --- | --- |
| 安装便利性 | httpx；mock 零配置 | 零依赖 | 需 Key+环境变量 |
| 真实 AI 接近度 | 高/中 | 低 | 高 |
| 演示可靠性 | 高（mock 保底） | 最高 | 低（断网/额度） |
| 费用 | 可控 | 免费 | 按次计费（192+） |

**选型理由**：`--mock` 用于管线和演示（确定性抽样 smart 75%→P3 34%）；在线模式经 config.json 接任意 OpenAI 兼容 API；直接 import `build_view` 保证 prompt 与网站字节级一致；`detect_mislead` 规则可复核。
**放弃**：仅 Mock（无法证明对真实 AI 有效，实际自测 48 格全 AC 反成证据）；仅在线（演示/调试依赖网络与额度）。

**风险与对策**：

| 风险 | 对策 |
| --- | --- |
| API Key 泄露 | Key 走环境变量（`os.environ.get(api_key_env)`），不入仓库；`.gitignore` 排除 `.env` |
| 代码提取失败（无代码块） | `extract_code()` 先找 ```python 块，再退化行段匹配；仍无则记 `NO_CODE` |

---

## 五、ASGI 服务器

| 维度 | **Uvicorn（已选）** | Gunicorn | Daphne |
| --- | --- | --- | --- |
| 安装便利性 | `pip install uvicorn` | 同 | 同 |
| 与 FastAPI 兼容 | 官方推荐 | 需适配器 | 通用 ASGI |
| Windows 支持 | 原生 | **不支持** | 支持 |
| 定位速度 | 快（终端日志） | 中（多进程分散） | 较慢（社区小） |

**选型理由**：FastAPI 官方推荐；**Windows 不支持 Gunicorn 是决定性因素**；启动命令一行搞定。
**放弃**：Gunicorn（不支持 Windows）；Daphne（为 WebSocket/HTTP2 设计，本项目过度）。

**风险与对策**：

| 风险 | 对策 |
| --- | --- |
| 端口 8000 被旧进程占用 | `start.py` 检测占用并提示；Windows 下用 `netstat` 排查，必要时改端口参数 |
| 后台启动日志不可见 | 重定向到 `server.log` / `server_err.log`，启动后 `Get-Content` 查看 |

---
